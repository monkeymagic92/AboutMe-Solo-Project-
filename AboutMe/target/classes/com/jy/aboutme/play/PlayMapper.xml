<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jy.aboutme.play.PlayMapper">
	
	<insert id="insGuest" parameterType="GuestVO">
		INSERT INTO t_guest
		(nickname, profile_image, email, gender, token, myIp, ip_addr, os, browser, agent)
		VALUES
		(#{nickname}, #{profile_image}, #{email}, #{gender}, #{token}, #{myIp}, #{ip_addr}, #{os}, #{browser}, #{agent})
	</insert>
	
	<select id="selEmail" parameterType="GuestVO" resultType="String">
	
		SELECT email FROM t_guest
		WHERE email = #{email}
		
	</select>
</mapper>